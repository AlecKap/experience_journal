<main class="container">
  <%= link_to sanitize("&larr; Back"), experience_entry_date_event_path(@experience, @entry_date, @event) %>

  <div class="header">
    <h1>Edit</h1>
  </div>
  
  <%= turbo_frame_tag @event do %>
    <%=  render "form", experience: @experience, entry_date: @entry_date, event: @event %>
  <% end %>
</main>