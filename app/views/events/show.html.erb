<main class="container">
  <%= link_to sanitize("&larr; Back to #{@experience.name}"), experience_entry_date_path(@experience, @entry_date) %>
  
  <div class="header">
    <h1>
      <%= @event.title %>
    </h1>
  </div>
  
  <%= render @description, description: @description %>
</main>